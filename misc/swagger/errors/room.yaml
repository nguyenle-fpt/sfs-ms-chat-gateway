create-room-failed:
  type: object
  description: 'Unable to create the room'
  properties:
    type:
      type: string
      format: 'create.room.failed'
    title:
      type: string
      format: 'Unable to create the room'
    status:
      type: integer
      format: 500

rename-room-failed:
  type: object
  description: 'Unable to rename the room'
  properties:
    type:
      type: string
      format: 'rename.room.failed'
    title:
      type: string
      format: 'Unable to rename room'
    status:
      type: integer
      format: 500

add-room-member-failed:
  type: object
  description: 'Unable to add the member in the room.'
  properties:
    type:
      type: string
      format: 'add.room.member.failed'
    title:
      type: string
      format: 'Unable to add the member in the room.'
    status:
      type: integer
      format: 500

reactivate-room-not-implemented:
  type: object
  description: 'Reactivate Room Not Implemented'
  properties:
    type:
      type: string
      format: 'reactivate.room.not.implemented'
    title:
      type: string
      format: 'Reactivate Room Not Implemented'
    status:
      type: integer
      format: 501


